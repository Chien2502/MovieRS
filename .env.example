# TMDb API Configuration
# Get your free API key at: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_api_key_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Set API_RELOAD=0 để tắt auto-reload khi chạy demo (mặc định 1)
# API_RELOAD=1

# CORS origins (phân tách bằng dấu phẩy, '*' = cho phép tất cả)
# CORS_ORIGINS=*

# Model Configuration
MODEL_PATH=models/model_latest.pkl
MODEL_VERSION=v1

# SVD Hyperparameters (retrain.py & train_pipeline.py đọc từ đây)
SVD_N_FACTORS=50
SVD_LR_ALL=0.005
SVD_REG_ALL=0.05
SVD_N_EPOCHS=20

# Model Gate: sai số RMSE cho phép khi so với model hiện tại (0.0 = không tệ hơn)
GATE_TOLERANCE=0.0

# Dataset Configuration
DATASET_SIZE=small  # 'small' for ml-latest-small, '25m' for ml-25m
